import { FC } from 'react';

import s from './SelectedWorksList.module.scss';

const SelectedWorksList: FC = () => {
  return <ul className={s.container}>{/*<SelectedWorksItem />*/}</ul>;
};
export default SelectedWorksList;
