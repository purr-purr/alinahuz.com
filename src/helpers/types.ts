export interface ISelectedWorks {
  poster: string;
  number: number;
  reverseState: boolean;
  title: string;
  description: string;
  implementation: string;
  link: string;
  screenShots: [{ image: string }];
}
